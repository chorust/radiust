[pytest]
asyncio_mode = auto
markers =
    live: requires explicit live-source opt-in
    provider: requires explicit object-storage provider configuration
    terminal: requires a terminal transcript or visual check
    source_adapter: directly exercises a registered provider adapter
addopts = -ra
