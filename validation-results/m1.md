# M1 validation

Status: partial

- Offline SDK/CLI contract, packaging import, NetCDF writer and local manifest tests pass in the CPython 3.13 environment.
- The current CPython 3.13 macOS arm64 wheel imports outside the source tree, exposes the Rust object writer, and its CLI completes an isolated loopback S3-compatible download with `written=1` followed by `skipped=1`.
- Independent `netCDF4 1.7.4` readback passes. On 2026-09-20, CF Checker 4.1.0 validated the offline fixture output against CF-1.8 with zero errors or warnings after exposing the installed UDUNITS library through `DYLD_LIBRARY_PATH`.
- M1 cannot be closed while the required canonical `my` raw/discovery/native-geometry evidence remains blocked in `migration/blockers/my.md`.
